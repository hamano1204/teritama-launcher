# Teritama Launcher (テリタマランチャー)

<p align="center">
  <img src="teritama_icon.png" width="128" height="128" alt="Teritama Launcher Icon" />
</p>

WPF で開発された Windows 用の軽量でスタイリッシュなアプリケーションランチャーです。
システムトレイに常駐し、設定したグローバルホットキーからいつでもポップアップメニューを呼び出し、登録したショートカットやアプリ、URLを素早く起動できます。

---

## 主な機能

### 1. システムトレイ常駐 & ポップアップメニュー
* タスクバーを占有せず、システムトレイ（通知領域）に常駐します。
* グローバルホットキー（デフォルト: `Ctrl + Shift + Win + R`）を押すと、マウスカーソルの位置にランチャーのポップアップメニューが表示されます。
* ポップアップ外をクリックするか、ESCキーを押すことで瞬時にメニューを閉じることができます。

### 2. 直感的でスムーズなキーボード/マウス操作
* **キーボード操作**: 矢印キー（`↑` `↓`）で項目を選択、`Enter` で決定（アプリケーション/URL起動）、`Esc` や `←` でメニューを閉じる / 親フォルダへ戻る。
* **マウス操作**: マウスホバーで瞬時にサブフォルダを展開。ホバー時のハイライト色とキーボード選択時のハイライト色（ブルー）が統一されています。

### 3. 多彩なツリー構造
* フォルダによる階層化（サブフォルダ対応）。空のフォルダがある場合は「（空）」と分かりやすく表示されます。
* 項目間の区切り線（セパレーター）をサポート。
* リスト内のアイコンもフラットなデザインで統一され、フォルダ（📁）、アプリ/URL（📄）、セパレーター（➖）が視覚的に区別しやすくなっています。

### 4. 高度なエディター機能
* ランチャー項目・フォルダ・セパレーターの追加、編集、削除。
* ドラッグ＆ドロップによる階層や順序の直感的な並べ替え。
* データのインポート / エクスポート機能（JSON形式）。

### 5. その他の設定
* グローバルホットキーのカスタマイズ（修飾キー Ctrl/Shift/Alt/Win + 任意のキー）。
* Windows ログイン時の自動起動（スタートアップ登録）の有効 / 無効切り替え。

---

## 動作環境

* **OS**: Windows 10 / 11
* **フレームワーク**: .NET 10 (WPF)

---

## インストール・ビルド方法

### 開発環境でのビルド手順

1. **リポジトリのクローン**:
   ```bash
   git clone https://github.com/hamano1204/teritama-launcher.git
   cd teritama-launcher
   ```

2. **ビルドと実行**:
   .NET 10 SDK がインストールされている環境で、以下のコマンドを実行します。
   ```bash
   dotnet build
   dotnet run
   ```

---

## 使い方

1. アプリケーションを起動すると、システムトレイ（タスクバーの右下）にテリタマランチャーのアイコンが表示されます。
2. ホットキー（デフォルト: `Ctrl + Shift + Win + R`）を押します。
3. ポップアップメニューから起動したいアプリケーションやURLを矢印キーまたはマウスで選択し、決定します。
4. 設定やランチャー項目の編集を行いたい場合は、システムトレイのアイコンをダブルクリックするか、右クリックメニューから「定型文編集」を選択してエディターウィンドウを開きます。

---

## 各種設定データの保存先
設定および登録データは、以下のディレクトリに `data.json` として保存されます。
* `%APPDATA%\TeritamaLauncher\data.json`

---

## ライセンス

このプロジェクトは MIT No Attribution (MIT-0) ライセンスのもとで公開されています。詳細は [LICENSE](LICENSE) ファイルをご覧ください。
